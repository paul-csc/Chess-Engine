# Zugzwang

A simple chess engine in C++

## Build

Using CMake:

```
git clone https://github.com/paul-csc/Chess-Engine.git Zugzwang
cd Zugzwang
cmake -S . -B build
cmake --build build
```
